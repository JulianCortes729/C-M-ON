# Normalización de fin de línea: el repo guarda LF, cada quien trabaja con lo suyo.
* text=auto

# ---------------------------------------------------------------------------
# Unity YAML — merge con UnityYAMLMerge
# Sin esto, dos personas tocando la misma escena produce un conflicto irresoluble:
# Git intenta mergear YAML por líneas y rompe las referencias por GUID.
# Requiere registrar el driver una vez por máquina (ver README de setup).
# ---------------------------------------------------------------------------
*.unity           merge=unityyamlmerge eol=lf
*.prefab          merge=unityyamlmerge eol=lf
*.asset           merge=unityyamlmerge eol=lf
*.meta            merge=unityyamlmerge eol=lf
*.mat             merge=unityyamlmerge eol=lf
*.anim            merge=unityyamlmerge eol=lf
*.controller      merge=unityyamlmerge eol=lf
*.overrideController merge=unityyamlmerge eol=lf
*.physicMaterial  merge=unityyamlmerge eol=lf
*.physicsMaterial merge=unityyamlmerge eol=lf
*.mask            merge=unityyamlmerge eol=lf
*.playable        merge=unityyamlmerge eol=lf
*.mixer           merge=unityyamlmerge eol=lf
*.terrainlayer    merge=unityyamlmerge eol=lf
*.signal          merge=unityyamlmerge eol=lf
*.brush           merge=unityyamlmerge eol=lf
*.lighting        merge=unityyamlmerge eol=lf
*.shadervariants  merge=unityyamlmerge eol=lf

# ---------------------------------------------------------------------------
# Texto
# ---------------------------------------------------------------------------
*.cs      text diff=csharp
*.shader  text
*.hlsl    text
*.cginc   text
*.compute text
*.json    text
*.md      text
*.txt     text
*.xml     text
*.yml     text
*.jslib   text
*.asmdef  text
*.uss     text
*.uxml    text

# ---------------------------------------------------------------------------
# Binarios: nunca normalizar ni diffear
# ---------------------------------------------------------------------------
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.tga  binary
*.psd  binary
*.tif  binary
*.exr  binary
*.hdr  binary
*.cubemap binary
*.fbx  binary
*.obj  binary
*.blend binary
*.dae  binary
*.3ds  binary
*.ttf  binary
*.otf  binary
*.wav  binary
*.mp3  binary
*.ogg  binary
*.aif  binary
*.mp4  binary
*.mov  binary
*.dll  binary
*.so   binary
*.dylib binary
*.pdf  binary
*.zip  binary
*.unitypackage binary

# Librerías de terceros: no contar en las estadísticas de lenguaje de GitHub
Assets/TextMesh?Pro/**       linguist-vendored
Assets/Plugins/**            linguist-vendored
Assets/ThirdParty/**         linguist-vendored
